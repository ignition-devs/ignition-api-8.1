[build-system]
build-backend = "setuptools.build_meta"
requires = [
  "setuptools>=42",
  "wheel",
]

[tool.commitizen]
name = "cz_conventional_commits"

[tool.isort]
profile = "black"
py_version = 27

[tool.mypy]
python_version = 2.7
