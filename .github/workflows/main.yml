name: main

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

jobs:
  test:
    uses: coatl-dev/workflows/.github/workflows/tox-docker.yml@v5.1.2
    with:
      pre-commit: true
